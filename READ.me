        Herança
*todas as classes são objetos
  -hashCode()
  -toString()
  -equals( Object obj )

       Observações sobre hashing
  h ( valor ) -> número
* dois objetos diferentes "podem" (nem sempredevem) gerar o mesmo hashing.
* dois objetos "iguais" ( entenda como "equals") devem obrigatoriamente garar o mesmo hashing.
